[package]
name = "sandbox-benchmarks"
version = "0.1.0"
edition = "2021"

[dependencies]

[dev-dependencies]
criterion = { version = "0.4", features = ["html_reports"] }
lazy_static = "1.4.0"
unshare = "0.7.0"
which = "4.4.0"

[[bench]]
name = "process"
harness = false
